# Offline Evaluation Configuration Parameters (LUCAS & Places365)

lucas:
  model_name: "vit_base_patch16_dinov3.lvd1689m"
  csv: "/path/to/Sen4Map_Metadata_test.csv"
  img_dir: "/path/to/LUCAS2018"
  eunis_raster: "/path/to/eunis_dominant.tif"
  env_zones_raster: "/path/to/eea_r_3035_100_m_EnvZ-Metzger_2025_v1_r00.tif"
  num_queries: 3000
  num_database: 0
  batch_size: 128
  seed: 42
  use_segformer: false
  use_fg_removal: true
  fg_attn_threshold: 1.5     # Multiplier over uniform attention (1/N) (e.g. 1.3 - 2.0)
  max_fg_ratio: 0.25         # Max fraction of patches allowed as foreground (25%)
  # ADE20K semantic segmentation classes to discard during background masking
  # (2: sky, 12: person, 20: car, 43: sign, 80: bus, 83: truck, 102: van, 127: bike)
  discard_classes: [2, 12, 20, 43, 80, 83, 102, 127]

places:
  model_name: "vit_base_patch16_dinov3.lvd1689m"
  labels: "/path/to/Scene_hierarchy.xlsx"
  img_dir: "/path/to/places365/val"
  num_queries: 3000
  num_database: 0
  batch_size: 128
  seed: 42
  use_segformer: false
  compare_clip: false
  use_fg_removal: true
  fg_attn_threshold: 1.5     # Multiplier over uniform attention (1/N) (e.g. 1.3 - 2.0)
  max_fg_ratio: 0.25         # Max fraction of patches allowed as foreground (25%)
  # ADE20K semantic segmentation classes to discard during background masking
  # (2: sky, 12: person, 20: car, 43: sign, 80: bus, 83: truck, 102: van, 127: bike)
  discard_classes: [2, 12, 20, 43, 80, 83, 102, 127]
