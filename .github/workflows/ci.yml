name: CI - Code Quality & Testing

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  lint-and-format:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Run Pre-Commit Hooks
        uses: pre-commit/action@v3.0.1

  unit-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install Dependencies
        run: |
          pip install --upgrade pip
          # Install core packages (wheels are pre-compiled and load instantly)
          pip install -r requirements.txt
          # Install CPU torch and timm for fast import checks
          pip install torch timm --index-url https://download.pytorch.org/whl/cpu

      - name: Run Unit Tests
        run: |
          PYTHONPATH=. python3 -m unittest discover -s tests
